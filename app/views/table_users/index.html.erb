<h1>Listing table_users</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @table_users.each do |table_user| %>
  <tr>
    <td><%= table_user.name %></td>
    <td><%= table_user.email %></td>
    <td><%= link_to 'Show', table_user %></td>
    <td><%= link_to 'Edit', edit_table_user_path(table_user) %></td>
    <td><%= link_to 'Destroy', table_user, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Table user', new_table_user_path %>
